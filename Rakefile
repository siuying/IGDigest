require 'rubygems'
require 'rake'

task :test do
  system 'xcodebuild -workspace IGDigest.xcworkspace -scheme IGDigest -sdk iphonesimulator9.2 test -destination \'platform=iOS Simulator,name=iPhone 6 Plus\''
end
